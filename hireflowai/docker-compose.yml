services:
  redis:
    image: redis:7.4-alpine
    container_name: hireflowai-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data: